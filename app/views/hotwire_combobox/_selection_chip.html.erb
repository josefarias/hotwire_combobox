<%# locals: (display:, value:, for_id:, data:) -%>

<%= turbo_stream.before for_id do %>
  <%= tag.div class: "hw-combobox__chip", data: data do %>
    <%= tag.span display %>
    <%= tag.span **combobox_chip_dismisser_attrs(value) %>
  <% end %>
<% end %>
