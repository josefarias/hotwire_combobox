<style>
  :root {
    --hw-option-bg-color: #edf1fa;
    --hw-active-bg-color: #aefa98;
    --hw-overflowing-animation: unset;
  }

  .hw-combobox__option--selected {
    background-color: #f4ff59;
  }
</style>

<%= hw_paginated_combobox_options @page.records,
      render_in: { partial: "movies/movie" },
      for_id: "movie-field",
      src: movies_html_path,
      next_page: @page.last? ? nil : @page.next_param %>
